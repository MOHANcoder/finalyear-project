export default function Card({direction,children}){
    return (
        <div
            style={{
                display:'flex',
                flexDirection:direction
            }}
        >
            
        </div>
    );
}